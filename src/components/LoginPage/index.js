import React from "react";
import AuthForm from "../AuthForm/index";

const LoginPage = () => {
  return (
    <React.Fragment>
      <AuthForm />
    </React.Fragment>
  );
};

export default LoginPage;
